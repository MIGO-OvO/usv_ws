# 工作周报 | 2026.05.22 - 2026.06.08

> 统计窗口：2026-05-22 00:00 +0800 至 2026-06-08 23:59:59 +0800。
> 统计对象：根仓库、`ardupilot-usv/`、`src/usv_ros/`、`WQ-USV-QGroundControl/`、`MotorControlApp_Pyside6/`、`docs/CGEDC/`，并排除 QGC `.cache/` 与 `build/cpm_modules/` 内的第三方依赖 Git 目录。
> 统计口径：以当前分支非合并提交为主；合并提交单列；变更规模为时间窗口开始前最近提交到当前 HEAD 的净 `git diff --shortstat`。未提交变更单独列为风险项。本报告不记录 remote URL。

---

## 1. 总览

### 1.1 仓库状态与变更规模

| 仓库 | 当前分支 | 非合并提交 | 合并提交 | 净变更规模 | 工作区状态 | 说明 |
|---|---:|---:|---:|---:|---|---|
| 根仓库 `usv_ws` | `codex/detector-stress-closure` | 4 | 0 | 10 files, +1098 -37 | 有未提交变更 | 文档、DetFirmware 健康包、压力测试控制 |
| `ardupilot-usv/` | `codex/unify-sampling-command-ardupilot` | 2 | 0 | 4 files, +24 -27 | 干净 | `NAV_SCRIPT_TIME` 采样闭环与健康遥测转发 |
| `src/usv_ros/` | `codex/detector-angle-freshness` | 16 | 14 | 55 files, +7068 -377 | 干净 | ROS/Web/前端/地图/仿真/健康遥测主变更区 |
| `WQ-USV-QGroundControl/` | `codex/qgc-build-dependency-lock` | 4 | 0 | 9 files, +170 -27 | 干净 | QGC 任务采样、健康遥测显示、依赖锁定 |
| `MotorControlApp_Pyside6/` | `codex/spectro-baseline-enhancements` | 0 | 0 | 无已提交净变更 | 有未提交变更 | Windows 上位机 baseline、健康监控、串口诊断工作线 |
| `docs/CGEDC/` | `main` | 2 | 0 | 新建资料仓库 | 有未提交变更 | 竞赛论文/图表/报名材料，不属于核心运行代码 |

本期已提交核心代码改动集中在 `src/usv_ros/`、`ardupilot-usv/`、`WQ-USV-QGroundControl/` 和根仓库跟踪的 `DetFirmware/`。`MotorControlApp_Pyside6/` 虽无时间窗内提交，但当前工作区已有较大未提交代码改动，不能视为正式入库成果。

### 1.2 本期主线

1. **航线定点采样改为飞控原生任务项闭环**：QGC 任务项使用 `MAV_CMD_NAV_SCRIPT_TIME(param1=1)`；飞控到点发送 `USV_SMPL`；ROS 完成采样后回 `USV_DONE`。
2. **系统健康遥测贯通三端**：ESP32 输出 0xEE 健康包，ROS 聚合 Jetson/ESP32/节点状态，bridge 转成 `NAMED_VALUE_FLOAT`，ArduPilot 缓存转发，QGC/Web 显示。
3. **Web 地图与实验室仿真增强**：新增离线瓦片缓存、离线包导入导出、离线模式，以及实验室虚拟任务仿真器。
4. **Jetson 部署与双网卡能力增强**：热点频段/信道可配置，外网与热点网卡角色分离，状态脚本输出默认路由来源。
5. **检测装置压力与韧性测试推进**：已提交压力测试控制与健康包；未提交部分继续补 I2C 超时、总线恢复和读数错误分型。
6. **Windows 上位机继续向独立 baseline/健康监控页演进**：当前为未提交改动，包含 baseline XLSX 导出、健康包解析、压力测试控制和串口 RTT 诊断。

---

## 2. MAVLink 与协议链路审计

本期涉及 MAVLink，已按规则先核对 `ardupilot-usv/` 固件源码。

### 2.1 航线采样闭环

| 环节 | 当前实现 | 源码依据 |
|---|---|---|
| QGC 任务项 | Rover 命令元数据把采样任务定义为 `MAV_CMD_NAV_SCRIPT_TIME`，说明 `USV_SMPL/USV_DONE` 闭环 | `WQ-USV-QGroundControl/src/MissionManager/MavCmdInfoRover.json:9-14` |
| 固件识别任务项 | Auto 模式处理 `MAV_CMD_NAV_SCRIPT_TIME` | `ardupilot-usv/Rover/mode_auto.cpp:543`、`:696` |
| 固件到点触发 | 到点后发送 `USV_SMPL`，携带 nav scripting id | `ardupilot-usv/Rover/mode_auto.cpp:1054` |
| ROS 接收触发 | bridge 在 `NAMED_VALUE_FLOAT` 中识别 `USV_SMPL` 并记录 `_fcu_sample_id` | `src/usv_ros/scripts/usv_mavlink_router_bridge.py:426-438` |
| ROS 执行采样 | trigger node 对飞控原生触发不切 HOLD、不做稳定等待，走 FCU sampling 流程 | `src/usv_ros/scripts/mavlink_trigger_node.py:848-858`、`:991-1018` |
| ROS 完成回告 | bridge 发送 `USV_DONE` 给飞控 | `src/usv_ros/scripts/usv_mavlink_router_bridge.py:382-389` |
| 固件接收完成 | 固件在 `NAMED_VALUE_FLOAT` 分支识别 `USV_DONE` | `ardupilot-usv/Rover/GCS_MAVLink_Rover.cpp:647` |

审计结论：任务采样链路已从“QGC/ROS 通过 31010 模拟任务触发”收敛为“飞控任务项触发 + ROS 业务完成回告”。`31010` 仍保留为手动开始采样命令，不再作为航线任务采样入口。

### 2.2 健康遥测链路

| 环节 | 当前实现 | 源码依据 |
|---|---|---|
| ESP32 健康包 | 新增 `HEADER2_HEALTH=0xEE`，`SystemHealthPacket` 固定 37 字节 | `DetFirmware/src/protocol_packets.h:14`、`:101-118` |
| ROS 泵控解析 | 泵控节点记录检测装置健康与角度缓存年龄 | `src/usv_ros/scripts/pump_control_node.py:1239-1241`、`:1291-1304`、`:1490-1491` |
| ROS 健康聚合 | `system_health_node.py` 聚合 Jetson、检测装置、ROS 节点健康 | `src/usv_ros/scripts/system_health_node.py:3`、`:230-237` |
| ROS 上行字段 | bridge 默认健康字段并写入 payload | `src/usv_ros/scripts/usv_mavlink_router_bridge.py:28`、`:310-322`、`:528-559` |
| 飞控缓存 | 固件缓存 `USV_JTMP/USV_ETMP/USV_JCPU/USV_JMEM/USV_EHEAP` | `ardupilot-usv/Rover/GCS_MAVLink_Rover.cpp:637-646` |
| 飞控转发 | 固件向 GCS 转发健康字段 | `ardupilot-usv/Rover/sensors.cpp:127-131` |
| QGC 显示 | QGC FactGroup 解析健康字段，面板显示 Jetson/ESP32/Heap | `WQ-USV-QGroundControl/custom/src/USVPayloadFactGroup.cc:197-205`、`custom/res/USVPayloadPanel.qml:541-549` |
| Web 显示 | Web store 接收 `system_health`，监控页显示角度新鲜度 | `src/usv_ros/frontend/src/store.ts:422`、`frontend/src/pages/Monitor.tsx:154-270` |

审计结论：健康字段名在 ROS、飞控、QGC 三端一致；当前健康遥测以 `NAMED_VALUE_FLOAT` 方式搭载，适合低频状态显示，不适合高频诊断流。

### 2.3 载荷字段转发

飞控当前在 `Rover/GCS_MAVLink_Rover.cpp:595-659` 缓存来自 companion 的 `NAMED_VALUE_FLOAT`，并在 `Rover/sensors.cpp:103-131` 过期保护后转发到 GCS。字段覆盖：

`USV_VOLT`、`USV_ABS`、`PUMP_X`、`PUMP_Y`、`PUMP_Z`、`PUMP_A`、`USV_STAT`、`USV_PKT`、`USV_STEP`、`USV_STOT`、`USV_SCNT`、`USV_PERR`、`USV_PMOD`、`USV_BSET`、`USV_REF`、`USV_BASE`、`USV_VLD`、`USV_JTMP`、`USV_ETMP`、`USV_JCPU`、`USV_JMEM`、`USV_EHEAP`。

审计结论：本期健康字段使载荷遥测从 17 项扩展为 22 项；QGC 契约测试已检查新增健康字段名，见 `WQ-USV-QGroundControl/custom/tests/test_usv_qgc_contract.py:28`。

---

## 3. 分仓库工作内容

### 3.1 根仓库与 `DetFirmware/`

提交清单：

| commit | 日期 | 说明 |
|---|---|---|
| `8e7ce16` | 2026-05-25 | Document unified sampling command flow |
| `96073b0` | 2026-06-03 | Feat: add detector health telemetry docs |
| `9fe2c48` | 2026-06-04 | Feat: add detector stress test controls |
| `32ef506` | 2026-06-04 | Docs: add May 8-22 software report |

主要改动：

- 文档更新：`docs/current/10_agent.md`、`20_system_overview.md`、`40_interfaces.md`、`70_verification.md` 等同步航线采样闭环、健康遥测和验证矩阵。
- `DetFirmware/src/protocol_packets.h` 增加 0xEE 系统健康包定义，含 uptime、温度、CPU、heap、任务数、任务栈水位。
- `DetFirmware/src/main.cpp` 增加 1 Hz 健康包发送、压力测试任务、`STRESS:START/STATUS?/STOP` 文本控制、FULL 模式虚拟 angle/spectro/PID 包输出。
- `DetFirmware/tests/test_stress_source.py` 增加源码级压力测试契约，检查压力命令优先于电机解析、双核低优先级任务、运动互斥和虚拟负载。

当前未提交变更：

- `DetFirmware/src/i2c_mux.h` 增加 `I2CReadStatus` 与 `readMt6701AngleWithStatus()`，区分 TCA mux 失败和 MT6701 设备读失败。
- `DetFirmware/src/main.cpp` 增加 `Wire.setTimeOut(15ms)`、SCL 脉冲恢复、连续 mux 失败阈值恢复、传感器任务每通道喂 watchdog。
- 压力任务从 task watchdog 监控中移除，避免低优先级压力任务干扰核心健康判断。
- 新增未跟踪测试 `DetFirmware/tests/test_i2c_resilience_source.py`。
- 新增未跟踪文档 `docs/HISTORY/session_0522.md`。

风险：这些 I2C 韧性改动尚未提交，也未做 ESP32 实机验证；若要纳入正式周报成果，应先独立提交并进行串口/I2C 故障注入测试。

### 3.2 `ardupilot-usv/`

提交清单：

| commit | 日期 | 说明 |
|---|---|---|
| `55326f56b3` | 2026-05-25 | Trigger sampling from NAV_SCRIPT_TIME |
| `b3821b6da4` | 2026-06-03 | Feat: forward system health telemetry |

主要改动：

- `Rover/mode_auto.cpp` 用 `MAV_CMD_NAV_SCRIPT_TIME` 触发 `USV_SMPL`，减少 ROS/QGC 对任务采样的自定义命令依赖。
- `Rover/Rover.h:449-474` 扩展 `usv_payload` 缓存结构，加入 Jetson/ESP32 健康字段。
- `Rover/GCS_MAVLink_Rover.cpp:595-647` 接收并缓存健康字段，同时保留 `USV_DONE` 完成闭环。
- `Rover/sensors.cpp:103-131` 在 3 秒缓存超时保护下转发载荷与健康字段。

风险：当前仓库干净，但未在本次审计中运行 WSL `waf` 构建；需要 Pixhawk6C 目标编译确认。

### 3.3 `src/usv_ros/`

非合并提交清单：

| commit | 日期 | 说明 |
|---|---|---|
| `aa5aa143` | 2026-05-22 | fix payload bridge keepalive reconnect |
| `95f9f003` | 2026-05-24 | Fix boot service install start gate |
| `9e77d0a5` | 2026-05-25 | Unify ROS sampling lifecycle events |
| `77c6bc1d` | 2026-05-26 | Add hotspot band controls and access commands |
| `4b4e0e46` | 2026-05-26 | Skip virtual bridge addresses in usvaddr |
| `6252c797` | 2026-05-26 | fix: sync sampling state across ros clients |
| `37b9da54` | 2026-05-26 | fix: sync sampling state across ros clients |
| `849af015` | 2026-05-27 | add amap pollution mapping |
| `203a9cca` | 2026-05-28 | fix web static asset fallback |
| `5ae1b1d6` | 2026-05-30 | feat(map): 离线地图缓存 |
| `4629e662` | 2026-05-30 | feat(map): 离线瓦片包导出/导入 + 离线模式 |
| `14b3444c` | 2026-05-30 | feat(lab): 虚拟任务仿真器 |
| `20932404` | 2026-06-01 | Fix web map cache and lab route editing |
| `15e06c9e` | 2026-06-03 | Feat: add system health telemetry |
| `334a10ac` | 2026-06-03 | Feat: assign dual wifi adapter bands |
| `4711dda4` | 2026-06-07 | Fix detector angle freshness telemetry |

主要改动：

- 采样状态：`mavlink_trigger_node.py`、`usv_mavlink_router_bridge.py` 修复采样开始/停止状态同步，区分 Web/手动/FCU 原生任务触发。
- 部署脚本：`install_boot_service.sh`、`setup_hotspot.sh`、`status_usv_all.sh` 增加热点频段、信道、route metric、默认路由来源诊断和双网卡角色配置。
- 地图能力：`Map.tsx`、`map_tile_cache.py`、`map_pack_export.py`、`map_pack_import.py` 增加高德栅格瓦片本地代理、离线缓存、离线包导入导出、弱网离线模式。
- 实验室仿真：`Lab.tsx`、`lab_sim_node.py`、`web_config_server.py` 增加虚拟航线制导、到点采样和模拟污染数据。
- 健康遥测：新增 `scripts/system_health_node.py` 和 `frontend/src/components/system-health-card.tsx`，`launch/usv_bringup.launch:60`、`:101-102` 默认启用健康节点。
- 角度新鲜度：`pump_control_node.py` 发布 `/usv/pump_angle_telemetry`，Web 保留 `raw_angles`，前端显示 ROS 与检测装置角度年龄，避免陈旧角度污染实时角度值。
- 静态前端产物：多次更新 `static/dist/`，与前端源码变更同步。

风险：

- `6252c797` 与 `37b9da54` 同名且相邻，需确认是否为重复 cherry-pick 或历史合并造成的重复提交。
- `static/dist` 被纳入提交，后续每次前端源码变更必须同步构建产物，否则部署页面可能与源码不一致。
- 未在本次审计中运行 ROS pytest、py_compile 或 Jetson 实机 launch。

### 3.4 `WQ-USV-QGroundControl/`

提交清单：

| commit | 日期 | 说明 |
|---|---|---|
| `ae129548c` | 2026-05-25 | Use NAV_SCRIPT_TIME for sampling missions |
| `c03ba1d62` | 2026-05-26 | fix: allow payload sampling restart |
| `85f2866b3` | 2026-06-03 | Feat: show system health telemetry |
| `6f176b50a` | 2026-06-04 | Build: lock QGC ulog dependency |

主要改动：

- `src/MissionManager/MavCmdInfoRover.json:9-14` 将航线采样任务项改为 `MAV_CMD_NAV_SCRIPT_TIME`，说明 `USV_SMPL/USV_DONE`。
- `custom/res/USVPayloadPanel.qml:39-48` 保留手动载荷命令 `31010..31019`，并修复采样完成/hold 后可重新开始。
- `custom/src/USVPayloadFactGroup.cc:197-205`、`custom/res/USVPayloadPanel.qml:541-549` 增加 Jetson/ESP32/Heap 健康显示。
- `custom/tests/test_usv_qgc_contract.py:198-215` 覆盖 Rover 命令元数据：任务采样不再用 31010，baseline/分光命令仍存在。
- `cmake/CustomOptions.cmake:167-169` 锁定 CPM cache 与 `ulog_cpp` tag，`src/AnalyzeView/CMakeLists.txt:61-72` 使用固定依赖。

风险：依赖锁定需要在 Qt/CMake 环境重新 configure/build 验证；本次审计未运行 QGC 构建。

### 3.5 `MotorControlApp_Pyside6/`

时间窗口内无提交，但当前工作区有未提交变更：

- 已跟踪修改 11 个文件，统计为 +358 -168。
- 未跟踪新增 8 个文件：`src/core/health_monitor.py`、`src/core/serial_diagnostics.py`、`src/ui/mixins/baseline_mixin.py`、`src/ui/mixins/health_monitor_mixin.py`、`tests/test_baseline_page_ui.py`、`tests/test_health_monitor.py`、`tests/test_health_monitor_ui.py`、`tests/test_serial_diagnostics.py`。

主要内容：

- `serial_reader.py` 增加 0xEE 健康包解析与 `health_packet_received` 信号。
- `spectro_baseline.py` 增加 baseline 导出记录、summary/samples 表生成，支撑 XLSX 导出。
- `baseline_mixin.py` 将 baseline 稳定性测试拆成独立页面，支持结果卡片和 XLSX 导出。
- `health_monitor_mixin.py` 增加 ESP32 健康监控页、CSV 导出、图表保存、压力测试命令、RTT/角度年龄记录。
- `serial_diagnostics.py` 增加检测装置 debug 文本解析和命令 RTT 追踪。
- `main_window_complete.py` 接入 Baseline/HealthMonitor mixin，新增导航按钮和页签。
- `main.py` 删除 Windows DPI awareness 调用，避免启动阶段覆盖 Qt DPI 行为。

风险：改动未提交、未跑 UI 测试；`main.py` DPI 行为变化需 Windows 实机确认。

### 3.6 `docs/CGEDC/`

提交清单：

| commit | 日期 | 说明 |
|---|---|---|
| `c2defd0` | 2026-05-22 | Restore CGEDC contest materials |
| `d97ea44` | 2026-05-22 | Rewrite technical paper using attachment 4 template |

当前未提交变更包括论文 doc/docx、摘要/章节重写 Markdown、第五章重构稿、图表生成输出、drawio 产物和 `tools/generate_paper_figures.py`。

审计结论：该仓库属于比赛资料区，不是本期核心运行代码；但未提交产物很多，应在交付前清理 Office 临时/备份文件并单独提交。

---

## 4. 测试与验证状态

本次审计执行了：

- `git status --short`：核对各仓库工作区状态。
- `git log --since/--until`：核对 2026-05-22 至 2026-06-08 提交。
- `git diff --shortstat`、`git diff --name-status`：核对净变更规模与未提交文件。
- `rg -n`：核对 MAVLink、健康遥测、DetFirmware、QGC、ROS、Windows 上位机关键源码行。

本次审计未执行：

- ArduPilot `waf` 固件构建。
- QGC Qt/CMake 构建。
- ROS pytest、py_compile、Jetson 实机 launch。
- Windows PySide6 UI 测试。
- ESP32 实机烧录、I2C 故障注入、压力测试实测。

---

## 5. 风险与建议

| 风险 | 等级 | 影响 | 建议 |
|---|---|---|---|
| 根仓库有未提交 DetFirmware I2C 韧性改动 | 高 | 改动涉及总线恢复、watchdog 与传感器读数路径，未实机会有稳定性风险 | 单独提交前做 ESP32 串口压力与 I2C 故障注入 |
| `MotorControlApp_Pyside6/` 大量未提交改动 | 高 | baseline/健康监控成果尚未进入历史，容易丢失或与后续改动冲突 | 先跑相关 pytest，再在该子仓库内单独提交 |
| ROS 前端源码和 `static/dist` 需同步 | 中 | 部署页面可能使用旧构建产物 | 每次前端改动记录构建并检查 dist hash |
| QGC 依赖锁定未构建验证 | 中 | CMake/CPM 配置可能在干净环境失败 | 在 Qt 构建环境重新 configure/build |
| ArduPilot 未构建 | 中 | `NAV_SCRIPT_TIME` 与健康字段改动虽小，但仍可能有编译目标差异 | WSL Ubuntu 执行 Pixhawk6C rover 构建 |
| 健康遥测走 `NAMED_VALUE_FLOAT` | 中 | 字段多、低频、无结构化 schema，长期扩展受限 | 后续评估自定义 MAVLink message 或 DEBUG_VECT 分组 |
| `docs/CGEDC/` 未提交产物多 | 低 | 文档仓库状态混乱，影响交付追踪 | 清理备份/临时文件，按论文、图表、工具分组提交 |

---

## 6. 下周期建议

1. 先收敛未提交工作区：根仓库 `DetFirmware` I2C 韧性改动、`MotorControlApp_Pyside6` baseline/健康监控、`docs/CGEDC` 文档产物分别在对应仓库独立提交。
2. 对 MAVLink 任务采样做一次实链路验证：QGC mission item -> 飞控 `USV_SMPL` -> ROS 采样 -> `USV_DONE` -> mission resume。
3. 对健康遥测做一次端到端验证：ESP32 0xEE -> ROS `/usv/system_health` -> bridge `NAMED_VALUE_FLOAT` -> ArduPilot 转发 -> QGC/Web 显示。
4. Jetson 实机验证双网卡热点：`HOTSPOT_IFACE=wlan1`、`INTERNET_IFACE=wlan0`、默认路由不走热点。
5. 运行仓库级验证命令：

```bash
cd src/usv_ros
python -m pytest tests/test_mavlink_command_compat.py tests/test_hardware_runtime_sync.py tests/test_boot_service_scripts.py tests/test_system_health_node.py
python -m py_compile scripts/pump_control_node.py scripts/usv_mavlink_router_bridge.py scripts/mavlink_trigger_node.py scripts/web_config_server.py scripts/system_health_node.py
```

```bash
cd ardupilot-usv
./waf configure --board Pixhawk6C
./waf rover
```

```bash
cd WQ-USV-QGroundControl
python custom/tests/test_usv_qgc_contract.py
```

```bash
cd MotorControlApp_Pyside6
python -m pytest tests/test_spectro_baseline_analysis.py tests/test_serial_reader_spectro_packet.py tests/test_health_monitor.py tests/test_serial_diagnostics.py tests/test_baseline_page_ui.py tests/test_health_monitor_ui.py
```

---

## 7. 关键源码索引

| 主题 | 文件 |
|---|---|
| 协同规则 | `docs/current/10_agent.md` |
| 接口事实源 | `docs/current/40_interfaces.md` |
| 飞控任务采样 | `ardupilot-usv/Rover/mode_auto.cpp` |
| 飞控载荷缓存 | `ardupilot-usv/Rover/GCS_MAVLink_Rover.cpp` |
| 飞控载荷转发 | `ardupilot-usv/Rover/sensors.cpp` |
| ROS MAVLink bridge | `src/usv_ros/scripts/usv_mavlink_router_bridge.py` |
| ROS trigger | `src/usv_ros/scripts/mavlink_trigger_node.py` |
| ROS 泵控/检测装置解析 | `src/usv_ros/scripts/pump_control_node.py` |
| ROS 健康聚合 | `src/usv_ros/scripts/system_health_node.py` |
| Web API/Socket.IO | `src/usv_ros/scripts/web_config_server.py` |
| QGC FactGroup | `WQ-USV-QGroundControl/custom/src/USVPayloadFactGroup.cc` |
| QGC 载荷面板 | `WQ-USV-QGroundControl/custom/res/USVPayloadPanel.qml` |
| QGC Rover 命令元数据 | `WQ-USV-QGroundControl/src/MissionManager/MavCmdInfoRover.json` |
| ESP32 协议包 | `DetFirmware/src/protocol_packets.h` |
| ESP32 主控逻辑 | `DetFirmware/src/main.cpp` |
| ESP32 I2C mux | `DetFirmware/src/i2c_mux.h` |
| Windows baseline | `MotorControlApp_Pyside6/src/core/spectro_baseline.py` |
| Windows 健康监控 | `MotorControlApp_Pyside6/src/core/health_monitor.py` |
