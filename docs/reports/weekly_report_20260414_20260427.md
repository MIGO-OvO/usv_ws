# 双周工作周报 | 2026.04.14 — 2026.04.27

> 统计周期内各仓库累计提交：QGC 6 次、ROS 25 次、ArduPilot 2 次、DetFirmware 11 次、Windows 上位机 1 次；合计 **45 次提交**。

---

## 1. 软件进展

### 1.1 QGC 地面站（6 commits / +1316 −10 行 vs v0.2.0-stable）

| commit | 日期 | 说明 |
|--------|------|------|
| f19ae35 | 04-23 | 在 QGC Plan 视图注册 USV 采样任务指令（stage 1） |
| bbf8b8e | 04-22 | 放宽伴随计算机心跳匹配条件，解决 bridge 心跳被丢弃问题 |
| e9c6393 | 04-22 | 修复采样数据页面销毁时 LineSeries 未清理导致堆异常 |
| bdd14cc | 04-22 | 图表仅在载荷活跃状态下采集数据，避免无效采样 |
| ec9da4f | 04-22 | 杂项更新 |
| 95a72c0 | 04-22 | 扩展任务阶段状态显示（stage 4），USVPayloadPanel 支持 13 种任务阶段文本 |

**要点：**
- **[新增]** `USVSamplingDataView.qml` 采样数据独立顶层页面上线，含实时电压/吸光度曲线、泵组状态与 PID 监控。
- **[新增]** QGC Plan 视图已注册 `USV_SMPL`、`USV_SURV` 自定义任务指令。
- **[修复]** 伴随计算机心跳过滤过严 → 放宽 component ID 匹配。
- **[修复]** 页面销毁堆错误 → 在 `Component.onDestruction` 中清空 `LineSeries`。
- **[优化]** 任务阶段编码从 4 种扩展到 13 种，QGC 面板实时显示航点采样全流程状态。

### 1.2 ROS 机载端（25 commits / +2546 −469 行 vs v0.2.0-stable）

| commit | 日期 | 说明 |
|--------|------|------|
| 3d9575e | 04-26 | bridge 接收异常不退出、launch 中 bridge 前置、下发分光等待超时 |
| 8ec7af9 | 04-26 | 自动化步骤等待新 valid 分光样本 |
| 8d248aa | 04-26 | 泵串口 reader 异常后自动重连 |
| 5c0cd0b | 04-26 | Web API JSON 判空与边界校验 |
| cb364a0 | 04-26 | bridge 发送统一到主循环，pending 队列有界 |
| 087025f | 04-26 | 消除 COMMAND_LONG 双入口重复执行 |
| 8d2d32e | 04-25 | 检测装置串口握手（`HELLO?` → `DET_ID`） |
| 5e6a0a3 | 04-23 | 前端静态文件更新 |
| 196cf2c | 04-23 | FCU 触发采样不再调用 `set_mode(HOLD)` |
| 2ec4be6 | 04-23 | 移除 ROS 侧模式劫持，飞控拥有任务流控制权（stage 3） |
| f4c0aa2 | 04-23 | bridge 支持 `USV_SMPL`/`USV_SURV`/`USV_DONE` FCU 原生采样触发（stage 2） |
| ffa582f | 04-23 | 采样拆分为手动/航点/巡航三模式（stage 1） |
| 9a62ca1 | 04-23 | WaypointSamplingCard 移至右列 |
| ce41515 | 04-22 | 航点同步三级 fallback + 自动应用硬件配置 |
| bdc5abd | 04-22 | 加固航点同步 fallback 策略 |
| 1dea706 | 04-22 | 修复泵角度在 QGC 端始终为 0 + 航点同步失败 |
| dbb6e83 | 04-22 | Web 端从 MAVROS mission 同步航点采样配置 |
| 696fe7a | 04-22 | stage 1~4 全链路代码审查修复 |
| 895eb2a | 04-22 | bridge 扩展 USV_STAT 编码支持 13 种任务阶段 |
| ae680cc | 04-22 | Web 航点采样 CRUD、编辑器、配置同步 |
| 7146900 | 04-22 | trigger 自动采样闭环（stage 1） |
| 6db5059 | 04-15 | bridge 新增 USV_STEP/STOT/SCNT/PERR/PMOD 遥测字段 |

**要点：**
- **[新增]** 自动采样任务全链路闭环：手动/航点/巡航三模式，FCU 原生任务指令触发。
- **[新增]** 13 个 `NAMED_VALUE_FLOAT` 遥测字段（含步骤进度、PID 状态、样本计数）。
- **[新增]** 检测装置串口握手识别机制，防止误连其他 USB 设备。
- **[新增]** 自动化步骤分光等待门槛（ADS 采集中时等待 valid 分光包）。
- **[修复]** COMMAND_LONG 双入口重复执行、bridge 并发 socket 写、Web API 空 JSON 越界。
- **[修复]** 泵串口 reader 异常后无法自动恢复 → 异步线程重连。
- **[优化]** launch 启动顺序：bridge 在 trigger 前声明，避免早期指令丢失。

### 1.3 ArduPilot 飞控固件（2 commits / +63 行 vs v0.2.0-stable）

| commit | 日期 | 说明 |
|--------|------|------|
| 160f647 | 04-26 | `usv_payload` 扩展 5 字段，2Hz 中继 13 个 NAMED_VALUE_FLOAT |
| 51bb7ee | 04-23 | 新增 `USV_SMPL`/`USV_SURV` 原生采样任务指令支持 |

**要点：**
- **[新增]** `GCS_MAVLink_Rover.cpp` 缓存 `USV_STEP/STOT/SCNT/PERR/PMOD`，`sensors.cpp` 2Hz 转发。
- **[新增]** `ModeAuto` 支持 `NAV_SCRIPT_TIME` 触发 `USV_SMPL`/`USV_SURV`，`USV_DONE` 回传完成信号。
- **源码依据：** `Rover.h:L459-465`、`GCS_MAVLink_Rover.cpp:L619-632`、`sensors.cpp:L100-123`。

### 1.4 检测装置固件 DetFirmware（11 commits）

| commit | 日期 | 说明 |
|--------|------|------|
| 3fe2378 | 04-26 | 启用 ESP32 任务看门狗（loop/TaskComms/TaskSensors） |
| 06502a9 | 04-26 | 串口命令长度限制 + 电机速度/角度钳位 |
| 8585dc8 | 04-25 | 串口命令解析与 I2C 传感器读取解耦 |
| d22e1d8 | 04-25 | 新增 `HELLO?`/`DET?` 身份握手协议 |
| 559b0a2 | 04-25 | 握手协议文档 |
| e3459d3 | 04-22 | 自动采样任务计划与接口文档同步 |
| bc06b6d | 04-26 | 接口文档同步 |
| 6839393 | 04-26 | Launch 顺序文档同步 |
| cbb5b6f | 04-26 | 看门狗文档同步 |

**要点：**
- **[新增]** 身份握手协议：`HELLO?\r\n` → `DET_ID:USV_DETECTOR,FW=2026.04.25,BAUD=115200`。
- **[新增]** ESP32 TWDT 5 秒超时看门狗，覆盖 `loop()`、`TaskComms()`、`TaskSensors()` 三个任务。
- **[修复]** 串口输入 buffer 无硬长度限制 → `MAX_COMMAND_LENGTH=160`，溢出丢弃至换行。
- **[修复]** 电机参数未钳位 → `MAX_OPEN_LOOP_RPM=20.0`、`MAX_COMMAND_DEGREES=3600.0`。
- **[优化]** `TaskComms` 与 `TaskSensors` 通过 `i2cMutex` 解耦，互不阻塞。
- **验证：** `pio run` 编译通过（RAM 7.5% / Flash 24.7%）。

### 1.5 Windows 上位机 MotorControlApp_Pyside6（1 commit）

| commit | 日期 | 说明 |
|--------|------|------|
| 289b4ad | 04-26 | `serial_mixin.send_command()` 自动补 `\r\n` 并 `flush()` |

**要点：**
- **[修复]** `serial_mixin.send_command()` 与 `serial_manager.py` 行为不一致 → 统一追加 `\r\n` + `flush()`。
- 串口连接已集成 `HELLO?` 握手识别（用户已有改动，未在本周期提交）。

---

## 2. 硬件进展

| 项目 | 状态 | 说明 |
|------|------|------|
| 监测装置 I2C 模块 PCB | ⚠️ 部分完成 | 完成焊接；磁编码器损坏，完整功能测试未完成 |
| 无人船设备仓 | ✅ 完成 | 完成改进设计 → 3D 打印 → 装配成功 |

---

## 3. 集成与测试

- **MAVLink 三端一致性审查**（04-26）：静态代码审查覆盖 QGC、ROS、ArduPilot、DetFirmware、Windows 上位机五端，识别 3 个高风险 + 6 个中风险 + 若干低风险问题，全部按优先级修复并提交。
- **飞控遥测链路验证**：ROS bridge 发送 13 个 `NAMED_VALUE_FLOAT` → 飞控缓存 → 2Hz 中继 → QGC 解析，链路逻辑已通过代码审查确认一致。
- **DetFirmware 构建验证**：`pio run` 编译通过（ESP32 NodeMCU-32S / framework-arduinoespressif32 @ 3.20017）。
- **ROS 端 Python 语法验证**：`py_compile` 覆盖 `pump_control_node.py`、`usv_mavlink_router_bridge.py`、`mavlink_trigger_node.py`、`web_config_server.py`，全部通过。
- **Launch XML 验证**：`xml.etree.ElementTree.parse()` 验证 `usv_bringup.launch` 结构正确。
- **注意：** ArduPilot 固件未运行 `waf` 构建，需用户在 WSL Ubuntu 环境验证。

---

## 4. 下周计划

1. **ArduPilot 固件构建验证**：在 WSL 环境执行 `./waf configure --board Pixhawk6C && ./waf rover`，确认遥测扩展编译通过。
2. **磁编码器更换**：更换损坏磁编码器后完成 I2C 模块 PCB 完整功能测试。
3. **联调测试**：上电联调 ROS → 飞控 → QGC 全链路 13 字段遥测，确认数据到达 QGC 面板。
4. **FCU 采样任务实测**：验证 `NAV_SCRIPT_TIME` → `USV_SMPL` → 采样 → `USV_DONE` 闭环流程。
5. **Web 前端打包**：更新前端静态构建物，部署至 Jetson Nano。
6. **数传电台链路监控**（roadmap #8）：bridge 接收 `RADIO_STATUS`，Web/QGC 显示电台信号质量。

---

## 5. 风险与阻塞

| 风险 | 等级 | 影响 | 缓解措施 |
|------|------|------|----------|
| 磁编码器损坏 | 🔴 高 | 监测装置 I2C 模块 PCB 完整测试受阻 | 已订购替换件，到货后优先测试 |
| ArduPilot 固件未构建验证 | 🟡 中 | 遥测字段扩展可能存在编译问题 | 下周 WSL 环境构建验证 |
| Windows 上位机握手改动未提交 | 🟡 中 | 用户本地改动与已提交修复可能冲突 | 用户需自行整理并提交 |
| 实机联调尚未进行 | 🟡 中 | 代码审查修复均为离线验证 | 联调为下周最高优先级 |
